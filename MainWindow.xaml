<Window x:Class="LinkOpener.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Link Opener" 
        Height="720" Width="620"
        MinHeight="720" MinWidth="620"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Select File" FontWeight="SemiBold" FontSize="14" 
                          Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="FilePathBox" Grid.Column="0" IsReadOnly="True" 
                     VerticalContentAlignment="Center" Background="#FFFFFF"  Foreground="{StaticResource TextBrush}" BorderBrush="#CCCCCC" Padding="10,8" Margin="0,0,10,0"/>
                    <Button x:Name="BrowseButton" Grid.Column="1" Content="Browse"
                            Style="{StaticResource PrimaryButton}" Click="BrowseButton_Click"/>
                </Grid>
                <TextBlock Text="Or drag &amp; drop a .txt file here" FontStyle="Italic" 
                          FontSize="11" Foreground="{StaticResource SubtleBrush}"
                          Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Browser" FontWeight="SemiBold" FontSize="14" 
                          Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>
                <StackPanel x:Name="BrowserPanel" Orientation="Horizontal"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Options" FontWeight="SemiBold" FontSize="14" 
                          Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox x:Name="PrivateModeCheckBox" Grid.Column="0"
                             Content="Private/Incognito Mode" Foreground="{StaticResource TextBrush}"/>
                    <CheckBox x:Name="RememberCheckBox" Grid.Column="1"
                             Content="Remember my preferences" Foreground="{StaticResource TextBrush}"/>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Preview" FontWeight="SemiBold" FontSize="14" 
                          Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>
                <TextBox x:Name="PreviewBox" Height="150" TextWrapping="Wrap"
                    AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                    IsReadOnly="True" FontFamily="Consolas" FontSize="10"
                    Background="#FFFFFF" Foreground="{StaticResource TextBrush}"
                     BorderBrush="#CCCCCC" Padding="10" Text="No file selected..."/>
            </StackPanel>
        </Border>

        <Button x:Name="OpenButton" Grid.Row="4" Content="OPEN"
                FontSize="16" FontWeight="Bold" Height="50" MinWidth="160"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Style="{StaticResource SuccessButton}" Click="OpenButton_Click"/>

        <TextBlock x:Name="StatusBar" Grid.Row="5" Text="Ready"
                  FontSize="11" Foreground="{StaticResource SubtleBrush}" Margin="0,15,0,0"/>
    </Grid>
</Window>